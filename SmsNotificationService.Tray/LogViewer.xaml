<Window x:Class="SmsNotificationService.Tray.LogViewer"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Logs — SmsNotificationService" Height="450" Width="650"
        WindowStartupLocation="CenterScreen" ShowInTaskbar="False">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Filter:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox x:Name="FilterCombo" Width="120" SelectionChanged="FilterCombo_SelectionChanged">
                <ComboBoxItem Content="All" IsSelected="True"/>
                <ComboBoxItem Content="App"/>
                <ComboBoxItem Content="Config"/>
                <ComboBoxItem Content="DB"/>
                <ComboBoxItem Content="Listener"/>
                <ComboBoxItem Content="Queue"/>
                <ComboBoxItem Content="SMS"/>
            </ComboBox>
        </StackPanel>

        <TextBox Grid.Row="2" x:Name="LogTextBox" IsReadOnly="True" VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto" TextWrapping="NoWrap"
                 FontFamily="Consolas" FontSize="11"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Refresh" Click="RefreshButton_Click" Margin="0,0,8,0" Padding="12,4"/>
            <Button Content="Clear" Click="ClearButton_Click" Margin="0,0,8,0" Padding="12,4"/>
            <Button Content="Export" Click="ExportButton_Click" Margin="0,0,8,0" Padding="12,4"/>
            <Button Content="Open Folder" Click="OpenFolderButton_Click" Padding="12,4"/>
        </StackPanel>
    </Grid>
</Window>
