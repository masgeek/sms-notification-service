name: .NET Agent Tests

on:
  push:
    branches: [main, develop]
  pull_request:

jobs:
  test-agent:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up .NET
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore tests/Agent/FeeSyncer.Agent.Tests.csproj

      - name: Test agent
        run: dotnet test tests/Agent/FeeSyncer.Agent.Tests.csproj --no-restore --configuration Release
